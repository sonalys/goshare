meta {
  name: Get
  type: http
  seq: 2
}

get {
  url: {{api_url}}/ledgers/:ledger_id/members
  body: none
  auth: inherit
}

params:path {
  ledger_id: {{ledger_id}}
}

vars:post-response {
  participant1: res.body.members[0].user_id
  participant2: res.body.members[1].user_id
}
